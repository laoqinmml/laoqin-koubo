# 自动剪辑发布：固定配置

## 路径

- 素材根目录：`G:\0 视频未剪辑`（只读）。
- 编排工作目录：`C:\Users\NBAMA\Documents\自动剪辑`（`processed.json`、`.env`、`自动剪辑工作流.md` 都在这里）。
- KB Cut 项目归档：`E:\自动剪辑`（kbcut 的 PROJECT_ROOT）。

## 素材目录规则

- 只处理日期命名文件夹（如 `0903`、`2026-09-03`、`20260903`）。
- 日期文件夹必须包含两个及以上账号子文件夹（大号 / 小号1 / 小号2 / 小号 / 企业号）才执行；只含一个账号子文件夹的跳过。
- 每个账号子文件夹内放口播视频（mp4 / mov / m4v / mkv）；可选单独放人物照片（png / jpg / heic）触发图生图封面。

## 账号映射

- 大号 → 大号
- 小号1 → 小号1
- 小号 → 小号1
- 小号2 → 小号2
- 企业号 → 企业号

最终以 `yxer accounts list` 返回的账号组名为准。

## 发布结果检查与提醒（2026-09-09 确认）

- 正式 `publish` 拿到任务 ID 后，必须用 `yxer query records` / `yxer query details <task_set_id>` 回查任务状态，不能只看 CLI 提交成功。
- 全部成功：✅ 汇报平台 + 账号 + 任务 ID，并在 `processed.json` 标记 `published`。
- 任一失败：用 ❌️ 逐条提醒用户，写明平台、账号、`errorMessage`（如“违反社区规范禁止发笔记”= 账号被平台限制，需用户去平台侧处理）、任务 ID；失败任务不标已发布、不静默重试。
- 平台侧失败即使本地校验/dry-run 都通过，也必须如实以 ❌️ 上报，不能把“任务已创建”当成“发布成功”。

## 渲染前预览门与色彩（2026-09-09 确认，2026-09-10 改为批次级）

- **批次级预览门**：本次任务（同一素材日期批次）里所有视频，先把封面和带字幕截图全部做好，一次性交给用户确认；用户一次确认后才统一批量渲染。禁止逐条确认、逐条渲染。
- 每条视频提供 6 张图：2 个封面（16:9 抽帧封面 + 3:4 生图/默认封面）＋ 4 张带字幕包装截图（开头 3s、中段前、中段后、末段）。用户口径为「2 个封面 + 3 张带字幕截图」，多出的第 6 张默认补一张中段带字幕截图。
- 预览截图统一保存到 `<项目>\工作文件\预览截图\`，不要写进交付目录；交付目录只放成片、封面、发布物料和 dbs 底稿。
- 字幕适用于所有画幅与所有风格：由逐词稿全量生成（只去纯语气词）、jieba 词边界断行、词级时间戳对时（详见 `$kbcut` 5.2.1）；每行字数上限取当前风格/画幅预算（`make-package` 打印的「每行上限 N 字」，founder 9:16≈13、16:9≈24），横屏/竖屏/其他 style 一律执行。
- 封面与截图都必须是最终色彩版本（iPhone HLG 源先转 SDR）；16:9 封面＝抽帧+标题模板，3:4 有照片时＝生图提示词直出。
- 未拿到用户确认前禁止执行 `hyperframes render`；确认后按批次统一整片渲染、压缩、发布物料。
- iPhone 等 HDR/HLG（BT.2020、arib-std-b67）素材，必须先用 ffmpeg 转成 SDR（BT.709、yuv420p）再进 make-package，禁止直接把 HDR 素材交给渲染，否则成片会发灰发白。
- 竖屏判断以 ffprobe 的显示方向为准（含旋转元数据的 1920×1080 源流按 1080×1920 处理为 9:16），不能只看流宽高。

## KB Cut 默认参数

- 风格：founder-interview。
- 画幅：竖屏素材 9:16，横屏素材 16:9。
- 3 秒钩子：保留拍摄时的原始开头顺序，不寻找、不前置金句（`hook_strategy: preserve_original_opening`、`hook_reorder_authorized: false`）。半自动同样默认不前置，除非用户当次明确要求。剪辑只做去气口、去废话、修口误、删重复，不改变叙事顺序。
- 裁切：全自动=居中/自动；半自动=看图确认。
- 响度：约 -23 LUFS（kbcut 默认）。
- 字幕：按「字幕断行与视觉规则」AI 语义断行、每行 ≤16 字、一句一段，不启用关键词黄色高亮。
- IP 介绍：横屏整段常驻；竖屏仅在 2–6 秒显示。
- 本地 dbs 打分：默认关闭。

## 字幕断行与视觉规则（2026-09-09 确认）

断行规则依据本机 AI剪口播 技能：`C:\Users\NBAMA\.codex\skills\AI剪口播\用户习惯\断行prompt.md`。

- 每行 ≤16 字；能单行完整就不拆。
- 断点优先级：句子/从句自然边界 > 连词前（但是、所以、因为、然后、而且）> 话题转折处（其实、另外）> 动词前（主语留上行，谓语下行）。
- 同一行内不拆：动宾搭配、偏正短语、介词短语、数量词+名词、专有名词、“的”字结构。
- 两行不等时下行更长（梯形），避免上行只有两三字。
- 字幕文本去掉中英文标点（保留 GPT-4 这类英文连字符）；中英/中数之间不加空格，英文单词之间保留空格。
- 断行是 AI 语义处理，不能用纯字符/词级切分代替；由编排 agent 逐条口播稿处理，再按转写段落回填时间轴。
- 字幕不做关键词黄色高亮：`caption_emphasis` 填 `["__none__"]` 关闭自动提取。
- IP 介绍：横屏整段常驻；竖屏只在 2–6 秒出现（founder 模板已按画幅条件实现）。
- 封面默认出 16:9 与 3:4 两版；账号子文件夹提供单独人像照片时，3:4 改用 gpt-image-2 图生图（提示词模板 `cover-prompt-photography.md`，标题取该视频），16:9 仍用抽帧 + 标题模板。

## 横屏字号（2026-09-10 修正）

- 横屏（16:9）工程的 `layout_overrides` 固定为 `{"--title-size": "2.8cqw", "--caption-size": "2.8cqw"}`；IP 介绍（speaker）字号用 frame.md 的横屏默认值 `2.35cqw` / `1.79cqw`，不要覆盖。
- 禁止把竖屏覆盖值（`--ip-top`、`--speaker-name-size 4.2cqw`、`--speaker-detail-size 3.2cqw`、`--caption-zone-top/bottom 60/76cqh`）套到横屏工程：实测会让横屏字幕和 IP 介绍显著偏大，与既有横屏成片不一致。
- 横屏封面沿用既有工程参数；封面标题字号由 make-cover 按标题字数自动推导，不要手写 `--title-size` 覆盖。

## 一话题多账号：文案必须各不相同（2026-09-13 确认）

同一条口播会录 大号 / 小号1 / 小号2 / 企业号 多个机位（有的批次只有其中几个）。**四个账号不能共用同一套标题和文案**，必须按账号分别写：

- 封面标题（`cover_title`，两行）每个账号都不同；
- 片内话题锁（`topic_line_1` 三段 + `topic_line_2`）每个账号都不同；
- 强调词（`cover_emphasis`）跟着封面标题走；
- 项目文件夹后缀取该账号自己的封面标题（重命名项目时同步改写项目内绝对路径）；
- 封面（16:9 / 3:4）与预览截图必须用新文案重做，不能沿用同一话题其他账号的旧图。

写法上按账号区分语气，但事实必须与口播内容一致：大号偏观点判断，企业号偏交付/实操视角，小号偏踩坑经历与干货清单。示例（0910 批次，网站外包话题）：

| 账号 | 封面标题 | 话题锁 |
| --- | --- | --- |
| 大号 | 网站外包 / 权限拿走 | 网站外包 · 这三样 · 必须拿走 |
| 企业号 | 建站交付 / 三权必拿 | 企业建站 · 交付时 · 先要三权 |
| 小号1 | 外包建站 / 别被卡住 | 外包建站 · 最容易 · 被卡住 |
| 小号2 | 建站之前 / 先要三样 | 建站之前 · 先要 · 这三样 |

## 本机发布的失败排查与重试（2026-09-13 实测）

本机发布常见三类失败，处置方式不同：

| 报错 | 阶段 | 处置 |
| --- | --- | --- |
| `视频下载失败:<OSS URL>` | `upload/fail` | 平台侧抓不到蚁小二 OSS 上的文件。**先用 `ffmpeg -c copy -movflags +faststart` 重新封装成片**（内容哈希变了就会生成新的 OSS 对象），再重发；若换对象仍失败，转去检查该账号在客户端的授权状态 |
| `登录失效` / `获取用户信息失败登录失效` | `push/fail` | 账号在蚁小二客户端里的授权过期——需要用户重新登录/授权该账号，然后重发（重新授权后同一账号此前失败的抖音任务往往也一并恢复） |
| `ENOENT ... platform-service\\video\\*.mp4` | `push/fail` | 客户端本地下载中断，直接重发即可 |

重试纪律：

- 发布脚本必须按 **(素材, 平台)** 记录状态，重试只补失败项，已成功的自动跳过，绝不重复发布；
- 每次重试后都要用 `yxer query records` + `query details` 回查平台侧真实状态（`stageStatus=success` 且 `errorMessage` 为空才算成功），不能只看 CLI 返回的 OK；
- 抖音的成功率明显低于其他平台（2026-09-13 实测 33 个任务里首轮失败 8 个抖音），批量发布后要留一轮重试。

## 发布通道：一律本机发布（2026-09-13 确认）

- 所有平台、所有账号的发布**一律走本机发布**：`--publish-channel local --client-id Z-jzdLWSjV1Zmo6hTXpD9`。
- 不再使用云发布（云发布消耗蚁小二云发布流量，配额用尽会 403；本机发布走本机客户端与网络，不吃该配额）。
- 云发布只允许在用户当次明确要求时使用；不要因为「默认是 cloud」而走云发布。
- 本机发布前先确认蚁小二客户端在线；`validate` → `publish --dry-run` → 正式 `publish` 三步必须使用同一套通道参数。
- 本机发布依赖本机在定时时间点保持开机且客户端在线，排期时要考虑这一点。

### 定时发布也可以走本机提交（2026-09-14 实测）

**定时发布不要求改用云通道**：带 `scheduledTime` 的任务同样可以用本机通道提交，实测 60 条记录全部为 `publishChannel=local`（含未来定时任务）。

分工与硬条件：

| 环节 | 执行方 | 客户端要求 |
| --- | --- | --- |
| 上传素材、`validate`、`publish --dry-run`、创建定时任务 | 调 CLI 的机器（Agent 侧） | `validate` / `dry-run` **不检查**客户端；`publish` 提交时**必须**客户端在线 |
| 到点真正推送、从 OSS 拉取素材 | 本机蚁小二客户端 | 到点必须在线 |

**唯一硬条件：提交 `publish` 那一刻客户端必须在运行。** 客户端关闭时提交会直接返回

```
检测客户端设备未连接,请稍后再试
```

而 `validate` 与 `publish --dry-run` 仍然通过——所以脚本化发布时不要以「validate/dry-run 通过」判断环境就绪。

实操顺序：先检查蚁小二客户端进程 → 不在线就先启动 `D:\Program Files\yixiaoer\蚁小二4.0.exe`，等日志出现 `设备注册成功` 再提交 → 任务进队列，到点由客户端执行。

## 企业号小红书发布规则（2026-09-12 确认）

- **账号**：企业号小红书＝「杭州太有科技」，账号 ID `6aa54e0fc25b5d121bcefcd5`（属企业号组 `69e5c7238f77f8bd2b7ea403`）。旧 ID `6a6aa6a53bc5d1b566381f07` 已于 9 月封禁后失效，遇到旧 ID 一律按新 ID 处理。
- **发布通道**：一律走**本机发布**（`--publish-channel local --client-id Z-jzdLWSjV1Zmo6hTXpD9`），不使用云发布流量。
- **排期**：必须用 `scheduledTime` 定时，**错开非整点时段**（12:16、18:43、19:41、20:16、20:52、21:26、21:58、22:24、22:47），**同一天最多 6 篇**，超过则顺延到次日。
- **待发清单**：企业号成片只要没发过小红书，就补发；已发过的（含被平台判失败的除外）不重复发，避免同一账号出现重复笔记。
- 参考脚本：`_0902_work/publish_qiye_xhs.py`（本机通道＋定时＋每日上限）与 `_0901_work/publish_backlog_batch.py`。

## 封面与话题锁内容规则（2026-09-10 补）

- 横屏（16:9）话题锁第二行 `topic_line_2` 必须按本期内容定制（例：WordPress 那条用「没技术别自己搭」），不要固定写「我是老秦」；只有个人简介类内容才适合用「我是老秦」。
- 竖屏 3:4 封面标题用中文两行（6-8 字），避免中英混排或纯英文：make-cover 按 CJK 字宽推导字号，英文行会把整块字号压小（实测同一标题 8.71cqw → 纯中文 15.68cqw），导致不占满 80% 宽度、两端无法对齐。
- 同一条视频的 16:9 与 3:4 封面标题可以不同：16:9 可保留英文产品词（如 WordPress），3:4 换等义中文（如「网站被黑」）。
- 3:4 封面标题按 80% 画面宽、居中、两行两端对齐排版；字号不足此标准视为未达标。

## 个人 IP（创始人风格固定使用）

- 名称：老秦
- 身份：8 年外贸运营实操，现役新能源企业外贸运营主管

## 活人感写作

- 输入：原始转写稿。
- 输出：小红书标题 + 小红书正文文案。
- 用途：作为小红书平台发布物料，覆盖 kbcut 默认小红书文案；抖音、视频号、B 站、快手等其他平台继续用 kbcut 的标题 / 简介 / Tags。

## 生图封面（2026-09-10 已验证）

- 适用范围：账号子文件夹提供单独人像照片时，**只用于 3:4 封面**；16:9 封面继续用 KB Cut 抽帧 + 标题模板，不调用生图。
- Base URL：`https://jojocode.com`（`.env` 的 `JOJOCODE_BASE_URL`）。
- 模型：`gpt-image-2`（`.env` 的 `JOJOCODE_MODEL`）。
- 接口：`POST {BASE_URL}/v1/images/edits`，multipart 图生图；可用 `GET /v1/models` 自检。
- 鉴权：`Authorization: Bearer $JOJOCODE_API_KEY`，密钥只从编排目录 `.env` 读取，不回显、不提交。
- 提示词模板：[cover-prompt-photography.md](cover-prompt-photography.md)。标题两行替换为该视频自己的封面标题（如 082610 用「八年设计 / 转外贸」），字数标注同步改成实际字数，其余逐字保留。
- 参数：`size=1080x1440`（接口返回 1088x1440，交付前用 ffmpeg 缩到 1080x1440）、`quality=high`、`input_fidelity=high`、`n=1`。
- 调用方式：Windows 用系统 `curl.exe`（Python urllib 会因证书链报 `CERTIFICATE_VERIFY_FAILED`）；提示词用 UTF-8 文件传参 `-F "prompt=<prompt.txt"`，避免命令行编码问题：

```powershell
curl.exe -sS -X POST "$base/v1/images/edits" `
  -H "Authorization: Bearer $key" `
  -F "model=$model" -F "prompt=<$promptFile" `
  -F "size=1080x1440" -F "quality=high" -F "input_fidelity=high" `
  -F "image=@$photo;type=image/png" -o result.json
```

- 生成图已含标题文字，直接作为 3:4 封面交付；不要再叠加封面模板标题。
- 2026-09-10 验证记录：`GET /v1/models` 返回 `gpt-image-2` 与 `gpt-image-2-4k`；`/v1/images/edits` 接受 `size=1080x1440`、`quality=high`、`input_fidelity=high`。

## 定时排期（非整点，优先流量时段，间隔尽量拉开）

| 视频数量 | 发布时间 |
| --- | --- |
| 1 | 12:16 |
| 2 | 12:16、18:43 |
| 3 | 12:16、18:43、21:08 |
| 4 | 12:16、18:43、20:15、21:48 |
| 5 | 12:16、18:43、19:57、21:09、22:31 |

视频更多时，顺延到 12:00–13:00、18:00–22:30 内的非整点时段，尽量拉开。
